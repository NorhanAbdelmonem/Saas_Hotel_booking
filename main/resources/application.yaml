spring:

  application:
    name: hotel
  datasource:
    url: jdbc:mysql://localhost:3306/hotel?createDatabaseIfNotExist=true
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update      # ????? ??? schema ??? ?? ????? ??? ??? Entities
    show-sql: true           # ??? ??? SQL ?? ????????
  profiles:
    active: dev  # ????? ??????? ???????

  jwt:
    secret: ${JWT_SECRET}      # ????? JWT (???? ?? Environment Variable)
    accessTokenExpiration: 900000 # ?????? Access Token ???????? (15 ?????)
    refreshTokenExpiration: 604800 # ?????? Refresh Token ???????? (7 ????)


stripe:
  secretKey: ${STRIPE_SECRET_KEY}           # ????? Stripe (Environment Variable)
  webhookSecretKey: ${STRIPE_WEBHOOK_SECRET_KEY} # ????? Webhook ? Stripe
